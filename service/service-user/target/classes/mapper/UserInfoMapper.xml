<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.gmall.user.mapper.UserInfoMapper">



    <sql id="Base_Column_List">
        id,login_name,nick_name,
        passwd,name,phone_num,
        email,head_img,user_level
    </sql>
    <select id="getUserByLoginNameAndPasswd" resultType="com.atguigu.gmall.model.user.UserInfo">

            select * from user_info where login_name = #{loginName} and passwd=#{passwd}

    </select>
</mapper>
